# Node JS Assignment #3 Templating Engines

1. Create a npm project and install Express.js
2. Add two routes:
  - "/" => Holds a <form> that allows user to input their name
  - "/users" => Outputs an <ul> with the user names (or some error text)
